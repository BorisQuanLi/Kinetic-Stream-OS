# Demo credentials — matches MSSQL_SA_PASSWORD in docker-compose.yml.
# These are intentionally committed for out-of-the-box demo setup.
# Do NOT use in any real deployment.
DB_PASS=YourStrongPassword123
